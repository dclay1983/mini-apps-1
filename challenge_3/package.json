{
  "name": "multistep_checkout",
  "version": "1.0.0",
  "description": "A single-page app that takes a user through a series of forms that simulate a shopping cart checkout experience.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "./node_modules/.bin/babel client --out-dir public --presets @babel/preset-react --source-maps inline --watch",
    "start": "node server.js"
  },
  "author": "David Clay",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.7.0",
    "@babel/core": "^7.7.2",
    "@babel/preset-react": "^7.7.0"
  }
}
